it("result of the sum of 2 + 2 must be 4", () => {// it receives two parameters: description and then the function
  const a = 2;
  const b = 2;
  const result = a + b;

  expect(result).toEqual(5); // what do you expect to happen

});